require "spec_helper"

describe TokyoEvents do
  it "has a version number" do
    expect(TokyoEvents::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
